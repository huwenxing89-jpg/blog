-- MySQL dump 10.13  Distrib 8.0.26, for Win64 (x86_64)
--
-- Host: localhost    Database: blog
-- ------------------------------------------------------
-- Server version	8.0.26

/*!40101 SET @OLD_CHARACTER_SET_CLIENT=@@CHARACTER_SET_CLIENT */;
/*!40101 SET @OLD_CHARACTER_SET_RESULTS=@@CHARACTER_SET_RESULTS */;
/*!40101 SET @OLD_COLLATION_CONNECTION=@@COLLATION_CONNECTION */;
/*!50503 SET NAMES utf8mb4 */;
/*!40103 SET @OLD_TIME_ZONE=@@TIME_ZONE */;
/*!40103 SET TIME_ZONE='+00:00' */;
/*!40014 SET @OLD_UNIQUE_CHECKS=@@UNIQUE_CHECKS, UNIQUE_CHECKS=0 */;
/*!40014 SET @OLD_FOREIGN_KEY_CHECKS=@@FOREIGN_KEY_CHECKS, FOREIGN_KEY_CHECKS=0 */;
/*!40101 SET @OLD_SQL_MODE=@@SQL_MODE, SQL_MODE='NO_AUTO_VALUE_ON_ZERO' */;
/*!40111 SET @OLD_SQL_NOTES=@@SQL_NOTES, SQL_NOTES=0 */;

--
-- Current Database: `blog`
--

/*!40000 DROP DATABASE IF EXISTS `blog`*/;

CREATE DATABASE /*!32312 IF NOT EXISTS*/ `blog` /*!40100 DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci */ /*!80016 DEFAULT ENCRYPTION='N' */;

USE `blog`;

--
-- Table structure for table `categories`
--

DROP TABLE IF EXISTS `categories`;
/*!40101 SET @saved_cs_client     = @@character_set_client */;
/*!50503 SET character_set_client = utf8mb4 */;
CREATE TABLE `categories` (
  `id` bigint NOT NULL AUTO_INCREMENT,
  `name` varchar(100) COLLATE utf8mb4_unicode_ci NOT NULL,
  `slug` varchar(100) COLLATE utf8mb4_unicode_ci NOT NULL,
  `description` text COLLATE utf8mb4_unicode_ci,
  `sort_order` int DEFAULT '0',
  `created_at` datetime DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  UNIQUE KEY `slug` (`slug`)
) ENGINE=InnoDB AUTO_INCREMENT=7 DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
/*!40101 SET character_set_client = @saved_cs_client */;

--
-- Dumping data for table `categories`
--

LOCK TABLES `categories` WRITE;
/*!40000 ALTER TABLE `categories` DISABLE KEYS */;
INSERT INTO `categories` (`id`, `name`, `slug`, `description`, `sort_order`, `created_at`) VALUES (1,'后端开发','back','后端开发',1,'2026-03-11 16:50:38'),(6,'前端开发','front','前端开发',0,'2026-03-12 20:00:10');
/*!40000 ALTER TABLE `categories` ENABLE KEYS */;
UNLOCK TABLES;

--
-- Table structure for table `post_tags`
--

DROP TABLE IF EXISTS `post_tags`;
/*!40101 SET @saved_cs_client     = @@character_set_client */;
/*!50503 SET character_set_client = utf8mb4 */;
CREATE TABLE `post_tags` (
  `post_id` bigint NOT NULL,
  `tag_id` bigint NOT NULL,
  PRIMARY KEY (`post_id`,`tag_id`),
  KEY `post_tags_ibfk_2` (`tag_id`),
  CONSTRAINT `post_tags_ibfk_1` FOREIGN KEY (`post_id`) REFERENCES `posts` (`id`) ON DELETE RESTRICT,
  CONSTRAINT `post_tags_ibfk_2` FOREIGN KEY (`tag_id`) REFERENCES `tags` (`id`) ON DELETE RESTRICT
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
/*!40101 SET character_set_client = @saved_cs_client */;

--
-- Dumping data for table `post_tags`
--

LOCK TABLES `post_tags` WRITE;
/*!40000 ALTER TABLE `post_tags` DISABLE KEYS */;
INSERT INTO `post_tags` (`post_id`, `tag_id`) VALUES (5,5),(5,6),(6,6);
/*!40000 ALTER TABLE `post_tags` ENABLE KEYS */;
UNLOCK TABLES;

--
-- Table structure for table `posts`
--

DROP TABLE IF EXISTS `posts`;
/*!40101 SET @saved_cs_client     = @@character_set_client */;
/*!50503 SET character_set_client = utf8mb4 */;
CREATE TABLE `posts` (
  `id` bigint NOT NULL AUTO_INCREMENT,
  `title` varchar(255) COLLATE utf8mb4_unicode_ci NOT NULL,
  `slug` varchar(255) COLLATE utf8mb4_unicode_ci NOT NULL,
  `content` longtext COLLATE utf8mb4_unicode_ci,
  `excerpt` text COLLATE utf8mb4_unicode_ci,
  `cover_image` varchar(500) COLLATE utf8mb4_unicode_ci DEFAULT NULL,
  `category_id` bigint DEFAULT NULL,
  `series_id` bigint DEFAULT NULL,
  `is_featured` tinyint(1) DEFAULT '0',
  `status` varchar(50) COLLATE utf8mb4_unicode_ci DEFAULT 'DRAFT',
  `view_count` int DEFAULT '0',
  `word_count` int DEFAULT '0',
  `published_at` datetime DEFAULT NULL,
  `scheduled_at` datetime DEFAULT NULL,
  `is_deleted` tinyint(1) DEFAULT '0',
  `deleted_at` datetime DEFAULT NULL,
  `created_at` datetime DEFAULT CURRENT_TIMESTAMP,
  `updated_at` datetime DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  UNIQUE KEY `slug` (`slug`),
  KEY `category_id` (`category_id`),
  KEY `series_id` (`series_id`),
  CONSTRAINT `posts_ibfk_1` FOREIGN KEY (`category_id`) REFERENCES `categories` (`id`),
  CONSTRAINT `posts_ibfk_2` FOREIGN KEY (`series_id`) REFERENCES `series` (`id`)
) ENGINE=InnoDB AUTO_INCREMENT=7 DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
/*!40101 SET character_set_client = @saved_cs_client */;

--
-- Dumping data for table `posts`
--

LOCK TABLES `posts` WRITE;
/*!40000 ALTER TABLE `posts` DISABLE KEYS */;
INSERT INTO `posts` (`id`, `title`, `slug`, `content`, `excerpt`, `cover_image`, `category_id`, `series_id`, `is_featured`, `status`, `view_count`, `word_count`, `published_at`, `scheduled_at`, `is_deleted`, `deleted_at`, `created_at`, `updated_at`) VALUES (1,'React Hooks Guide','react-hooks-guide','# React Hooks Guide\n\nLearn React Hooks with examples.','A complete guide to React Hooks','https://images.unsplash.com/photo-1633356122544-f134324a6cee?w=800',NULL,NULL,0,'published',122,42,'2026-03-11 16:50:38',NULL,1,'2026-03-12 16:22:11','2026-03-11 16:50:38','2026-03-12 19:59:44'),(2,'Spring Boot Tutorial','spring-boot-tutorial','# Spring Boot Tutorial\n\nBuild REST APIs with Spring Boot.','Learn Spring Boot from scratch','https://images.unsplash.com/photo-1517694712202-14dd9538aa97?w=800',NULL,NULL,0,'published',217,45,'2026-03-11 16:50:38',NULL,1,'2026-03-12 16:22:13','2026-03-11 16:50:38','2026-03-12 19:59:44'),(3,'11','12','# Docker Basics\n14','13','https://images.unsplash.com/photo-1605745341112-85968b19335b?w=800',NULL,NULL,0,'published',70,14,'2026-03-11 16:50:38',NULL,1,'2026-03-12 16:22:14','2026-03-11 16:50:38','2026-03-12 19:59:15'),(4,'11222','222','```javascript\nfunction greet(name) {\n  return `Hello, ${name}!`;\n}\n\nconsole.log(greet(\'World\'));\n','233','',1,2,0,'draft',19,62,NULL,NULL,0,NULL,'2026-03-11 22:43:20','2026-03-11 22:43:20'),(5,'前端多线程实现大文件上传：从分片到秒传的完整方案','深入探索 Web Worker 多线程技术，实现高性能大文件分片上传、断点续传与秒传功能','## 前端多线程实现大文件上传\n在现代 Web 应用中，大文件上传是一个常见且复杂的需求。传统的单线程上传方式在处理 GB 级别文件时会导致页面卡顿、计算 Hash 耗时过长等问题。本文将深入探讨如何利用 Web Worker 多线程技术，实现高性能的大文件上传方案。\n\n### 核心挑战与解决思路#\n大文件上传面临的核心挑战：\n\n┌─────────────────────────────────────────────────────────┐\n│                    大文件上传核心挑战                    │\n├─────────────────────────────────────────────────────────┤\n│  1. 文件过大 ──────────► 网络超时、内存溢出               │  \n│  2. Hash 计算 ─────────► 主线程阻塞、页面卡顿             │\n│  3. 上传中断 ─────────► 需要重新上传、用户体验差          │\n│  4. 重复上传 ─────────► 带宽浪费、服务器存储冗余           │\n└─────────────────────────────────────────────────────────┘\n\n┌─────────────────────────────────────────────────────────┐\n│                      解决方案架构                        │\n├─────────────────────────────────────────────────────────┤\n│                                                         │\n│   ┌──────────┐    ┌──────────┐    ┌──────────┐          │\n│   │ 文件分片  │───►│ 多线程Hash│───►│ 并发上传  │         │\n│   └──────────┘    └──────────┘    └──────────┘             │\n│        │               │               │                    │\n│        ▼               ▼               ▼                    │\n│   解决内存问题     解决卡顿问题     提升上传速度               │\n│                                                             │\n│   ┌──────────┐    ┌──────────┐                             │\n│   │ 断点续传  │───►│  秒传    │                              │\n│   └──────────┘    └──────────┘                             │\n│        │               │                                    │\n│        ▼               ▼                                    │\n│   解决中断问题     解决重复问题                               │\n│                                                             │\n└─────────────────────────────────────────────────────────────┘\n### Web Worker 多线程基础\n#### 为什么需要 Web Worker\nJavaScript 是单线程语言，计算密集型任务会阻塞主线程：\n\n```typescript\n// ❌ 主线程计算 Hash - 页面卡死\nasync function calculateHashInMainThread(file: File): Promise<string> {\n  const buffer = await file.arrayBuffer()\n  const hashBuffer = await crypto.subtle.digest(\'SHA-256\', buffer)\n  // 大文件计算期间，页面完全无响应\n  return bufferToHex(hashBuffer)\n}\n\n// ✅ Web Worker 计算 Hash - 页面流畅\nfunction calculateHashWithWorker(file: File): Promise<string> {\n  return new Promise((resolve, reject) => {\n    const worker = new Worker(\'/workers/hash-worker.js\')\n    worker.postMessage(file)\n    worker.onmessage = (e) => {\n      resolve(e.data.hash)\n      worker.terminate()\n    }\n    worker.onerror = reject\n  })\n}\n```\n#### Web Worker 通信机制\n```typescript\n// 主线程与 Worker 的通信流程\ninterface WorkerMessage<T = any> {\n  type: \'START\' | \'PROGRESS\' | \'COMPLETE\' | \'ERROR\'\n  payload: T\n}\n\n// 主线程\nclass WorkerManager {\n  private worker: Worker\n  private callbacks: Map<string, Function> = new Map()\n\n  constructor(workerPath: string) {\n    this.worker = new Worker(workerPath)\n    this.setupListeners()\n  }\n\n  private setupListeners() {\n    this.worker.onmessage = (e: MessageEvent<WorkerMessage>) => {\n      const { type, payload } = e.data\n      const callback = this.callbacks.get(type)\n      callback?.(payload)\n    }\n\n    this.worker.onerror = (error) => {\n      console.error(\'Worker error:\', error)\n      const errorCallback = this.callbacks.get(\'ERROR\')\n      errorCallback?.(error)\n    }\n  }\n\n  send<T>(data: T, transfer?: Transferable[]) {\n    this.worker.postMessage(data, transfer || [])\n  }\n\n  on(type: string, callback: Function) {\n    this.callbacks.set(type, callback)\n  }\n\n  terminate() {\n    this.worker.terminate()\n  }\n}\n```\n### 文件分片策略\n#### 动态分片算法\n根据文件大小动态调整分片策略：\n```typescript\ninterface ChunkConfig {\n  chunkSize: number      // 分片大小\n  chunkCount: number     // 分片数量\n  parallelLimit: number  // 并发数量\n}\n\nfunction calculateChunkConfig(fileSize: number): ChunkConfig {\n  // 动态分片策略\n  const strategies = [\n    { maxSize: 100 * 1024 * 1024, chunkSize: 2 * 1024 * 1024, parallel: 3 },   // < 100MB\n    { maxSize: 500 * 1024 * 1024, chunkSize: 5 * 1024 * 1024, parallel: 5 },   // < 500MB\n    { maxSize: 1024 * 1024 * 1024, chunkSize: 10 * 1024 * 1024, parallel: 6 }, // < 1GB\n    { maxSize: Infinity, chunkSize: 20 * 1024 * 1024, parallel: 8 }            // >= 1GB\n  ]\n\n  const strategy = strategies.find(s => fileSize <= s.maxSize)!\n  \n  return {\n    chunkSize: strategy.chunkSize,\n    chunkCount: Math.ceil(fileSize / strategy.chunkSize),\n    parallelLimit: strategy.parallel\n  }\n}\n\n// 文件分片生成器\nfunction* createFileChunks(\n  file: File, \n  chunkSize: number\n): Generator<{ chunk: Blob; index: number; start: number; end: number }> {\n  let index = 0\n  let start = 0\n\n  while (start < file.size) {\n    const end = Math.min(start + chunkSize, file.size)\n    const chunk = file.slice(start, end)\n    \n    yield { chunk, index, start, end }\n    \n    index++\n    start = end\n  }\n}\n```\n#### 分片数据结构\n```typescript\ninterface FileChunk {\n  index: number          // 分片索引\n  chunk: Blob           // 分片数据\n  hash: string          // 分片 Hash\n  size: number          // 分片大小\n  start: number         // 起始位置\n  end: number           // 结束位置\n  status: ChunkStatus   // 上传状态\n  retries: number       // 重试次数\n  progress: number      // 上传进度\n}\n\nenum ChunkStatus {\n  PENDING = \'pending\',\n  UPLOADING = \'uploading\',\n  SUCCESS = \'success\',\n  ERROR = \'error\',\n  PAUSED = \'paused\'\n}\n\ninterface UploadFile {\n  file: File\n  fileHash: string           // 整文件 Hash\n  fileName: string\n  fileSize: number\n  chunks: FileChunk[]\n  status: \'pending\' | \'uploading\' | \'paused\' | \'success\' | \'error\'\n  progress: number\n  uploadedSize: number\n  startTime: number\n  speed: number              // 上传速度 bytes/s\n}\n```\n### 多线程 Hash 计算\n#### Hash Worker 实现\n使用 spark-md5 库在 Worker 中增量计算大文件 Hash：\n```typescript\n// workers/hash-worker.ts\nimport SparkMD5 from \'spark-md5\'\n\ninterface HashTask {\n  file: File\n  chunkSize: number\n}\n\ninterface HashProgress {\n  type: \'PROGRESS\' | \'COMPLETE\' | \'ERROR\'\n  progress?: number\n  hash?: string\n  chunkHashes?: string[]\n  error?: string\n}\n\nself.onmessage = async (e: MessageEvent<HashTask>) => {\n  const { file, chunkSize } = e.data\n  \n  try {\n    const result = await calculateFileHash(file, chunkSize)\n    self.postMessage({\n      type: \'COMPLETE\',\n      hash: result.fileHash,\n      chunkHashes: result.chunkHashes\n    } as HashProgress)\n  } catch (error) {\n    self.postMessage({\n      type: \'ERROR\',\n      error: (error as Error).message\n    } as HashProgress)\n  }\n}\n\nasync function calculateFileHash(\n  file: File, \n  chunkSize: number\n): Promise<{ fileHash: string; chunkHashes: string[] }> {\n  const spark = new SparkMD5.ArrayBuffer()\n  const chunkHashes: string[] = []\n  const totalChunks = Math.ceil(file.size / chunkSize)\n  \n  let currentChunk = 0\n\n  while (currentChunk < totalChunks) {\n    const start = currentChunk * chunkSize\n    const end = Math.min(start + chunkSize, file.size)\n    const chunk = file.slice(start, end)\n    \n    const buffer = await chunk.arrayBuffer()\n    \n    // 计算整文件 Hash\n    spark.append(buffer)\n    \n    // 计算分片 Hash\n    const chunkSpark = new SparkMD5.ArrayBuffer()\n    chunkSpark.append(buffer)\n    chunkHashes.push(chunkSpark.end())\n    \n    currentChunk++\n    \n    // 报告进度\n    self.postMessage({\n      type: \'PROGRESS\',\n      progress: Math.round((currentChunk / totalChunks) * 100)\n    } as HashProgress)\n  }\n\n  return {\n    fileHash: spark.end(),\n    chunkHashes\n  }\n}\n```\n#### 多 Worker 并行计算\n对于超大文件，使用多个 Worker 并行计算：\n\n```typescript\nclass ParallelHashCalculator {\n  private workerCount: number\n  private workers: Worker[] = []\n\n  constructor(workerCount = navigator.hardwareConcurrency || 4) {\n    this.workerCount = Math.min(workerCount, 8)\n  }\n\n  async calculate(file: File, chunkSize: number): Promise<string[]> {\n    const totalChunks = Math.ceil(file.size / chunkSize)\n    const chunksPerWorker = Math.ceil(totalChunks / this.workerCount)\n    \n    const tasks: Promise<string[]>[] = []\n\n    for (let i = 0; i < this.workerCount; i++) {\n      const startChunk = i * chunksPerWorker\n      const endChunk = Math.min(startChunk + chunksPerWorker, totalChunks)\n      \n      if (startChunk >= totalChunks) break\n      \n      tasks.push(this.calculateChunkRange(file, chunkSize, startChunk, endChunk))\n    }\n\n    const results = await Promise.all(tasks)\n    return results.flat()\n  }\n\n  private calculateChunkRange(\n    file: File,\n    chunkSize: number,\n    startChunk: number,\n    endChunk: number\n  ): Promise<string[]> {\n    return new Promise((resolve, reject) => {\n      const worker = new Worker(\'/workers/chunk-hash-worker.js\')\n      this.workers.push(worker)\n\n      worker.postMessage({\n        file,\n        chunkSize,\n        startChunk,\n        endChunk\n      })\n\n      worker.onmessage = (e) => {\n        if (e.data.type === \'COMPLETE\') {\n          resolve(e.data.hashes)\n          worker.terminate()\n        }\n      }\n\n      worker.onerror = (error) => {\n        reject(error)\n        worker.terminate()\n      }\n    })\n  }\n\n  terminate() {\n    this.workers.forEach(w => w.terminate())\n    this.workers = []\n  }\n}\n```\n### 并发上传控制\n#### 请求并发池\n```typescript\ninterface UploadTask {\n  chunk: FileChunk\n  fileHash: string\n  fileName: string\n  totalChunks: number\n}\n\nclass ConcurrencyPool {\n  private maxConcurrency: number\n  private currentCount = 0\n  private queue: Array<() => Promise<void>> = []\n  private paused = false\n\n  constructor(maxConcurrency: number) {\n    this.maxConcurrency = maxConcurrency\n  }\n\n  async add<T>(task: () => Promise<T>): Promise<T> {\n    return new Promise((resolve, reject) => {\n      const wrappedTask = async () => {\n        try {\n          const result = await task()\n          resolve(result)\n        } catch (error) {\n          reject(error)\n        } finally {\n          this.currentCount--\n          this.runNext()\n        }\n      }\n\n      if (this.paused) {\n        this.queue.push(wrappedTask)\n        return\n      }\n\n      if (this.currentCount < this.maxConcurrency) {\n        this.currentCount++\n        wrappedTask()\n      } else {\n        this.queue.push(wrappedTask)\n      }\n    })\n  }\n\n  private runNext() {\n    if (this.paused || this.queue.length === 0) return\n    if (this.currentCount >= this.maxConcurrency) return\n\n    const task = this.queue.shift()\n    if (task) {\n      this.currentCount++\n      task()\n    }\n  }\n\n  pause() {\n    this.paused = true\n  }\n\n  resume() {\n    this.paused = false\n    while (\n      this.currentCount < this.maxConcurrency && \n      this.queue.length > 0\n    ) {\n      this.runNext()\n    }\n  }\n\n  clear() {\n    this.queue = []\n  }\n\n  get pending() {\n    return this.queue.length\n  }\n\n  get running() {\n    return this.currentCount\n  }\n}\n```\n#### 分片上传请求\n```typescript\ninterface UploadChunkResponse {\n  success: boolean\n  chunkIndex: number\n  message?: string\n}\n\nasync function uploadChunk(\n  task: UploadTask,\n  onProgress: (loaded: number) => void,\n  signal?: AbortSignal\n): Promise<UploadChunkResponse> {\n  const formData = new FormData()\n  formData.append(\'file\', task.chunk.chunk)\n  formData.append(\'hash\', task.fileHash)\n  formData.append(\'chunkHash\', task.chunk.hash)\n  formData.append(\'chunkIndex\', String(task.chunk.index))\n  formData.append(\'totalChunks\', String(task.totalChunks))\n  formData.append(\'fileName\', task.fileName)\n\n  return new Promise((resolve, reject) => {\n    const xhr = new XMLHttpRequest()\n    \n    xhr.upload.onprogress = (e) => {\n      if (e.lengthComputable) {\n        onProgress(e.loaded)\n      }\n    }\n\n    xhr.onload = () => {\n      if (xhr.status >= 200 && xhr.status < 300) {\n        resolve(JSON.parse(xhr.responseText))\n      } else {\n        reject(new Error(`Upload failed: ${xhr.status}`))\n      }\n    }\n\n    xhr.onerror = () => reject(new Error(\'Network error\'))\n    xhr.onabort = () => reject(new Error(\'Upload aborted\'))\n\n    if (signal) {\n      signal.addEventListener(\'abort\', () => xhr.abort())\n    }\n\n    xhr.open(\'POST\', \'/api/upload/chunk\')\n    xhr.send(formData)\n  })\n}\n```\n### 断点续传实现\n#### 本地存储进度\n```typescript\ninterface UploadProgress {\n  fileHash: string\n  fileName: string\n  fileSize: number\n  chunkSize: number\n  uploadedChunks: number[]\n  createdAt: number\n  updatedAt: number\n}\n\nclass UploadProgressStore {\n  private storageKey = \'upload_progress_\'\n  private db: IDBDatabase | null = null\n\n  async init(): Promise<void> {\n    return new Promise((resolve, reject) => {\n      const request = indexedDB.open(\'FileUploadDB\', 1)\n\n      request.onerror = () => reject(request.error)\n      request.onsuccess = () => {\n        this.db = request.result\n        resolve()\n      }\n\n      request.onupgradeneeded = (event) => {\n        const db = (event.target as IDBOpenDBRequest).result\n        if (!db.objectStoreNames.contains(\'progress\')) {\n          const store = db.createObjectStore(\'progress\', { keyPath: \'fileHash\' })\n          store.createIndex(\'updatedAt\', \'updatedAt\')\n        }\n      }\n    })\n  }\n\n  async save(progress: UploadProgress): Promise<void> {\n    if (!this.db) await this.init()\n    \n    return new Promise((resolve, reject) => {\n      const transaction = this.db!.transaction([\'progress\'], \'readwrite\')\n      const store = transaction.objectStore(\'progress\')\n      \n      progress.updatedAt = Date.now()\n      const request = store.put(progress)\n      \n      request.onsuccess = () => resolve()\n      request.onerror = () => reject(request.error)\n    })\n  }\n\n  async get(fileHash: string): Promise<UploadProgress | null> {\n    if (!this.db) await this.init()\n    \n    return new Promise((resolve, reject) => {\n      const transaction = this.db!.transaction([\'progress\'], \'readonly\')\n      const store = transaction.objectStore(\'progress\')\n      const request = store.get(fileHash)\n      \n      request.onsuccess = () => resolve(request.result || null)\n      request.onerror = () => reject(request.error)\n    })\n  }\n\n  async delete(fileHash: string): Promise<void> {\n    if (!this.db) await this.init()\n    \n    return new Promise((resolve, reject) => {\n      const transaction = this.db!.transaction([\'progress\'], \'readwrite\')\n      const store = transaction.objectStore(\'progress\')\n      const request = store.delete(fileHash)\n      \n      request.onsuccess = () => resolve()\n      request.onerror = () => reject(request.error)\n    })\n  }\n\n  async cleanup(maxAge = 7 * 24 * 60 * 60 * 1000): Promise<void> {\n    if (!this.db) await this.init()\n    \n    const cutoff = Date.now() - maxAge\n    \n    return new Promise((resolve, reject) => {\n      const transaction = this.db!.transaction([\'progress\'], \'readwrite\')\n      const store = transaction.objectStore(\'progress\')\n      const index = store.index(\'updatedAt\')\n      const range = IDBKeyRange.upperBound(cutoff)\n      \n      const request = index.openCursor(range)\n      \n      request.onsuccess = (event) => {\n        const cursor = (event.target as IDBRequest).result\n        if (cursor) {\n          cursor.delete()\n          cursor.continue()\n        } else {\n          resolve()\n        }\n      }\n      \n      request.onerror = () => reject(request.error)\n    })\n  }\n}\n```\n#### 服务端校验已上传分片\n```typescript\ninterface VerifyResponse {\n  uploaded: boolean           // 文件是否已完全上传（秒传）\n  uploadedChunks: number[]    // 已上传的分片索引\n}\n\nasync function verifyUploadedChunks(\n  fileHash: string,\n  fileName: string,\n  totalChunks: number\n): Promise<VerifyResponse> {\n  const response = await fetch(\'/api/upload/verify\', {\n    method: \'POST\',\n    headers: { \'Content-Type\': \'application/json\' },\n    body: JSON.stringify({ fileHash, fileName, totalChunks })\n  })\n  \n  return response.json()\n}\n```\n### 完整上传器实现\nFileUploader 类\n```typescript\ninterface UploaderOptions {\n  chunkSize?: number\n  maxConcurrency?: number\n  maxRetries?: number\n  onProgress?: (progress: UploadProgressEvent) => void\n  onSuccess?: (response: UploadSuccessEvent) => void\n  onError?: (error: UploadErrorEvent) => void\n  onHashProgress?: (progress: number) => void\n}\n\ninterface UploadProgressEvent {\n  fileHash: string\n  fileName: string\n  progress: number\n  uploadedSize: number\n  totalSize: number\n  speed: number\n  remainingTime: number\n}\n\ninterface UploadSuccessEvent {\n  fileHash: string\n  fileName: string\n  url: string\n  instantUpload: boolean\n}\n\ninterface UploadErrorEvent {\n  fileHash: string\n  fileName: string\n  error: Error\n  chunk?: FileChunk\n}\n\nclass FileUploader {\n  private options: Required<UploaderOptions>\n  private pool: ConcurrencyPool\n  private store: UploadProgressStore\n  private abortController: AbortController | null = null\n  private uploadFile: UploadFile | null = null\n  private speedSamples: number[] = []\n  private lastUploadedSize = 0\n  private lastSampleTime = 0\n\n  constructor(options: UploaderOptions = {}) {\n    this.options = {\n      chunkSize: 5 * 1024 * 1024,\n      maxConcurrency: 5,\n      maxRetries: 3,\n      onProgress: () => {},\n      onSuccess: () => {},\n      onError: () => {},\n      onHashProgress: () => {},\n      ...options\n    }\n\n    this.pool = new ConcurrencyPool(this.options.maxConcurrency)\n    this.store = new UploadProgressStore()\n  }\n\n  async upload(file: File): Promise<void> {\n    try {\n      await this.store.init()\n      this.abortController = new AbortController()\n\n      // 1. 计算文件 Hash\n      const { fileHash, chunkHashes } = await this.calculateHash(file)\n\n      // 2. 验证已上传分片（秒传检查）\n      const config = calculateChunkConfig(file.size)\n      const verifyResult = await verifyUploadedChunks(\n        fileHash,\n        file.name,\n        config.chunkCount\n      )\n\n      // 秒传：文件已存在\n      if (verifyResult.uploaded) {\n        this.options.onSuccess({\n          fileHash,\n          fileName: file.name,\n          url: `/files/${fileHash}`,\n          instantUpload: true\n        })\n        return\n      }\n\n      // 3. 创建分片\n      const chunks = this.createChunks(file, config, chunkHashes, verifyResult.uploadedChunks)\n\n      // 4. 初始化上传状态\n      this.uploadFile = {\n        file,\n        fileHash,\n        fileName: file.name,\n        fileSize: file.size,\n        chunks,\n        status: \'uploading\',\n        progress: 0,\n        uploadedSize: this.calculateUploadedSize(chunks),\n        startTime: Date.now(),\n        speed: 0\n      }\n\n      // 5. 保存进度\n      await this.saveProgress()\n\n      // 6. 上传分片\n      await this.uploadChunks()\n\n      // 7. 合并分片\n      await this.mergeChunks(fileHash, file.name, chunks.length)\n\n      // 8. 清理进度\n      await this.store.delete(fileHash)\n\n      this.options.onSuccess({\n        fileHash,\n        fileName: file.name,\n        url: `/files/${fileHash}`,\n        instantUpload: false\n      })\n\n    } catch (error) {\n      if ((error as Error).message !== \'Upload aborted\') {\n        this.options.onError({\n          fileHash: this.uploadFile?.fileHash || \'\',\n          fileName: this.uploadFile?.fileName || file.name,\n          error: error as Error\n        })\n      }\n    }\n  }\n\n  private async calculateHash(file: File): Promise<{\n    fileHash: string\n    chunkHashes: string[]\n  }> {\n    return new Promise((resolve, reject) => {\n      const worker = new Worker(\'/workers/hash-worker.js\')\n\n      worker.postMessage({\n        file,\n        chunkSize: this.options.chunkSize\n      })\n\n      worker.onmessage = (e) => {\n        if (e.data.type === \'PROGRESS\') {\n          this.options.onHashProgress(e.data.progress)\n        } else if (e.data.type === \'COMPLETE\') {\n          resolve({\n            fileHash: e.data.hash,\n            chunkHashes: e.data.chunkHashes\n          })\n          worker.terminate()\n        } else if (e.data.type === \'ERROR\') {\n          reject(new Error(e.data.error))\n          worker.terminate()\n        }\n      }\n\n      worker.onerror = (error) => {\n        reject(error)\n        worker.terminate()\n      }\n    })\n  }\n\n  private createChunks(\n    file: File,\n    config: ChunkConfig,\n    chunkHashes: string[],\n    uploadedChunks: number[]\n  ): FileChunk[] {\n    const chunks: FileChunk[] = []\n    const uploadedSet = new Set(uploadedChunks)\n\n    for (const { chunk, index, start, end } of createFileChunks(file, config.chunkSize)) {\n      chunks.push({\n        index,\n        chunk,\n        hash: chunkHashes[index],\n        size: chunk.size,\n        start,\n        end,\n        status: uploadedSet.has(index) ? ChunkStatus.SUCCESS : ChunkStatus.PENDING,\n        retries: 0,\n        progress: uploadedSet.has(index) ? 100 : 0\n      })\n    }\n\n    return chunks\n  }\n\n  private async uploadChunks(): Promise<void> {\n    if (!this.uploadFile) return\n\n    const pendingChunks = this.uploadFile.chunks.filter(\n      c => c.status === ChunkStatus.PENDING || c.status === ChunkStatus.ERROR\n    )\n\n    const tasks = pendingChunks.map(chunk => \n      this.pool.add(() => this.uploadSingleChunk(chunk))\n    )\n\n    await Promise.all(tasks)\n  }\n\n  private async uploadSingleChunk(chunk: FileChunk): Promise<void> {\n    if (!this.uploadFile) return\n\n    chunk.status = ChunkStatus.UPLOADING\n\n    try {\n      await uploadChunk(\n        {\n          chunk,\n          fileHash: this.uploadFile.fileHash,\n          fileName: this.uploadFile.fileName,\n          totalChunks: this.uploadFile.chunks.length\n        },\n        (loaded) => {\n          chunk.progress = (loaded / chunk.size) * 100\n          this.updateProgress()\n        },\n        this.abortController?.signal\n      )\n\n      chunk.status = ChunkStatus.SUCCESS\n      chunk.progress = 100\n      await this.saveProgress()\n      this.updateProgress()\n\n    } catch (error) {\n      if ((error as Error).message === \'Upload aborted\') {\n        chunk.status = ChunkStatus.PAUSED\n        throw error\n      }\n\n      chunk.retries++\n      \n      if (chunk.retries < this.options.maxRetries) {\n        chunk.status = ChunkStatus.PENDING\n        await this.uploadSingleChunk(chunk)\n      } else {\n        chunk.status = ChunkStatus.ERROR\n        throw error\n      }\n    }\n  }\n\n  private async mergeChunks(\n    fileHash: string,\n    fileName: string,\n    totalChunks: number\n  ): Promise<void> {\n    const response = await fetch(\'/api/upload/merge\', {\n      method: \'POST\',\n      headers: { \'Content-Type\': \'application/json\' },\n      body: JSON.stringify({ fileHash, fileName, totalChunks })\n    })\n\n    if (!response.ok) {\n      throw new Error(\'Failed to merge chunks\')\n    }\n  }\n\n  private calculateUploadedSize(chunks: FileChunk[]): number {\n    return chunks\n      .filter(c => c.status === ChunkStatus.SUCCESS)\n      .reduce((sum, c) => sum + c.size, 0)\n  }\n\n  private updateProgress(): void {\n    if (!this.uploadFile) return\n\n    const uploadedSize = this.uploadFile.chunks.reduce((sum, chunk) => {\n      if (chunk.status === ChunkStatus.SUCCESS) {\n        return sum + chunk.size\n      }\n      return sum + (chunk.size * chunk.progress / 100)\n    }, 0)\n\n    const now = Date.now()\n    const timeDiff = (now - this.lastSampleTime) / 1000\n    \n    if (timeDiff >= 0.5) {\n      const bytesUploaded = uploadedSize - this.lastUploadedSize\n      const speed = bytesUploaded / timeDiff\n      \n      this.speedSamples.push(speed)\n      if (this.speedSamples.length > 5) {\n        this.speedSamples.shift()\n      }\n      \n      this.lastUploadedSize = uploadedSize\n      this.lastSampleTime = now\n    }\n\n    const avgSpeed = this.speedSamples.length > 0\n      ? this.speedSamples.reduce((a, b) => a + b, 0) / this.speedSamples.length\n      : 0\n\n    const remainingSize = this.uploadFile.fileSize - uploadedSize\n    const remainingTime = avgSpeed > 0 ? remainingSize / avgSpeed : 0\n\n    this.uploadFile.uploadedSize = uploadedSize\n    this.uploadFile.progress = (uploadedSize / this.uploadFile.fileSize) * 100\n    this.uploadFile.speed = avgSpeed\n\n    this.options.onProgress({\n      fileHash: this.uploadFile.fileHash,\n      fileName: this.uploadFile.fileName,\n      progress: this.uploadFile.progress,\n      uploadedSize,\n      totalSize: this.uploadFile.fileSize,\n      speed: avgSpeed,\n      remainingTime\n    })\n  }\n\n  private async saveProgress(): Promise<void> {\n    if (!this.uploadFile) return\n\n    await this.store.save({\n      fileHash: this.uploadFile.fileHash,\n      fileName: this.uploadFile.fileName,\n      fileSize: this.uploadFile.fileSize,\n      chunkSize: this.options.chunkSize,\n      uploadedChunks: this.uploadFile.chunks\n        .filter(c => c.status === ChunkStatus.SUCCESS)\n        .map(c => c.index),\n      createdAt: this.uploadFile.startTime,\n      updatedAt: Date.now()\n    })\n  }\n\n  pause(): void {\n    this.pool.pause()\n    this.abortController?.abort()\n    \n    if (this.uploadFile) {\n      this.uploadFile.status = \'paused\'\n      this.uploadFile.chunks\n        .filter(c => c.status === ChunkStatus.UPLOADING)\n        .forEach(c => c.status = ChunkStatus.PAUSED)\n    }\n  }\n\n  async resume(): Promise<void> {\n    if (!this.uploadFile) return\n\n    this.abortController = new AbortController()\n    this.uploadFile.status = \'uploading\'\n    this.uploadFile.chunks\n      .filter(c => c.status === ChunkStatus.PAUSED)\n      .forEach(c => c.status = ChunkStatus.PENDING)\n    \n    this.pool.resume()\n    await this.uploadChunks()\n  }\n\n  cancel(): void {\n    this.pause()\n    this.pool.clear()\n    \n    if (this.uploadFile) {\n      this.store.delete(this.uploadFile.fileHash)\n      this.uploadFile = null\n    }\n  }\n}\n```\n### React Hook 封装\n```typescript\nimport { useState, useCallback, useRef } from \'react\'\n\ninterface UseFileUploadOptions {\n  chunkSize?: number\n  maxConcurrency?: number\n  maxRetries?: number\n}\n\ninterface UploadState {\n  status: \'idle\' | \'hashing\' | \'uploading\' | \'paused\' | \'success\' | \'error\'\n  progress: number\n  hashProgress: number\n  speed: number\n  remainingTime: number\n  error: Error | null\n  instantUpload: boolean\n}\n\nexport function useFileUpload(options: UseFileUploadOptions = {}) {\n  const [state, setState] = useState<UploadState>({\n    status: \'idle\',\n    progress: 0,\n    hashProgress: 0,\n    speed: 0,\n    remainingTime: 0,\n    error: null,\n    instantUpload: false\n  })\n\n  const uploaderRef = useRef<FileUploader | null>(null)\n\n  const upload = useCallback(async (file: File) => {\n    setState(prev => ({ ...prev, status: \'hashing\', progress: 0, hashProgress: 0 }))\n\n    uploaderRef.current = new FileUploader({\n      ...options,\n      onHashProgress: (hashProgress) => {\n        setState(prev => ({ ...prev, hashProgress }))\n      },\n      onProgress: ({ progress, speed, remainingTime }) => {\n        setState(prev => ({\n          ...prev,\n          status: \'uploading\',\n          progress,\n          speed,\n          remainingTime\n        }))\n      },\n      onSuccess: ({ instantUpload }) => {\n        setState(prev => ({\n          ...prev,\n          status: \'success\',\n          progress: 100,\n          instantUpload\n        }))\n      },\n      onError: ({ error }) => {\n        setState(prev => ({\n          ...prev,\n          status: \'error\',\n          error\n        }))\n      }\n    })\n\n    await uploaderRef.current.upload(file)\n  }, [options])\n\n  const pause = useCallback(() => {\n    uploaderRef.current?.pause()\n    setState(prev => ({ ...prev, status: \'paused\' }))\n  }, [])\n\n  const resume = useCallback(async () => {\n    setState(prev => ({ ...prev, status: \'uploading\' }))\n    await uploaderRef.current?.resume()\n  }, [])\n\n  const cancel = useCallback(() => {\n    uploaderRef.current?.cancel()\n    setState({\n      status: \'idle\',\n      progress: 0,\n      hashProgress: 0,\n      speed: 0,\n      remainingTime: 0,\n      error: null,\n      instantUpload: false\n    })\n  }, [])\n\n  return {\n    ...state,\n    upload,\n    pause,\n    resume,\n    cancel\n  }\n}\n```\n### 上传组件示例\n```tsx\nimport { useFileUpload } from \'@/hooks/useFileUpload\'\nimport { formatBytes, formatTime } from \'@/utils/format\'\n\nexport function FileUploader() {\n  const {\n    status,\n    progress,\n    hashProgress,\n    speed,\n    remainingTime,\n    error,\n    instantUpload,\n    upload,\n    pause,\n    resume,\n    cancel\n  } = useFileUpload({\n    chunkSize: 5 * 1024 * 1024,\n    maxConcurrency: 5,\n    maxRetries: 3\n  })\n\n  const handleFileChange = async (e: React.ChangeEvent<HTMLInputElement>) => {\n    const file = e.target.files?.[0]\n    if (file) {\n      await upload(file)\n    }\n  }\n\n  return (\n    <div className=\"p-6 max-w-md mx-auto bg-white rounded-xl shadow-lg\">\n      <input\n        type=\"file\"\n        onChange={handleFileChange}\n        disabled={status === \'uploading\' || status === \'hashing\'}\n        className=\"mb-4\"\n      />\n\n      {status === \'hashing\' && (\n        <div className=\"mb-4\">\n          <p className=\"text-sm text-gray-600 mb-2\">正在计算文件指纹...</p>\n          <div className=\"h-2 bg-gray-200 rounded-full overflow-hidden\">\n            <div\n              className=\"h-full bg-blue-500 transition-all duration-300\"\n              style={{ width: `${hashProgress}%` }}\n            />\n          </div>\n          <p className=\"text-xs text-gray-500 mt-1\">{hashProgress}%</p>\n        </div>\n      )}\n\n      {(status === \'uploading\' || status === \'paused\') && (\n        <div className=\"mb-4\">\n          <div className=\"flex justify-between text-sm text-gray-600 mb-2\">\n            <span>上传进度</span>\n            <span>{progress.toFixed(1)}%</span>\n          </div>\n          <div className=\"h-2 bg-gray-200 rounded-full overflow-hidden\">\n            <div\n              className=\"h-full bg-green-500 transition-all duration-300\"\n              style={{ width: `${progress}%` }}\n            />\n          </div>\n          <div className=\"flex justify-between text-xs text-gray-500 mt-1\">\n            <span>速度: {formatBytes(speed)}/s</span>\n            <span>剩余: {formatTime(remainingTime)}</span>\n          </div>\n        </div>\n      )}\n\n      {status === \'success\' && (\n        <div className=\"p-4 bg-green-50 rounded-lg\">\n          <p className=\"text-green-600\">\n            {instantUpload ? \'秒传成功！\' : \'上传成功！\'}\n          </p>\n        </div>\n      )}\n\n      {status === \'error\' && (\n        <div className=\"p-4 bg-red-50 rounded-lg\">\n          <p className=\"text-red-600\">上传失败: {error?.message}</p>\n        </div>\n      )}\n\n      <div className=\"flex gap-2 mt-4\">\n        {status === \'uploading\' && (\n          <button\n            onClick={pause}\n            className=\"px-4 py-2 bg-yellow-500 text-white rounded hover:bg-yellow-600\"\n          >\n            暂停\n          </button>\n        )}\n        {status === \'paused\' && (\n          <button\n            onClick={resume}\n            className=\"px-4 py-2 bg-blue-500 text-white rounded hover:bg-blue-600\"\n          >\n            继续\n          </button>\n        )}\n        {(status === \'uploading\' || status === \'paused\') && (\n          <button\n            onClick={cancel}\n            className=\"px-4 py-2 bg-red-500 text-white rounded hover:bg-red-600\"\n          >\n            取消\n          </button>\n        )}\n      </div>\n    </div>\n  )\n}\n```\n### 服务端实现（Node.js）\n```typescript\n// api/upload/chunk.ts\nimport { NextApiRequest, NextApiResponse } from \'next\'\nimport formidable from \'formidable\'\nimport fs from \'fs/promises\'\nimport path from \'path\'\n\nexport const config = {\n  api: { bodyParser: false }\n}\n\nconst UPLOAD_DIR = path.join(process.cwd(), \'uploads\')\n\nexport default async function handler(\n  req: NextApiRequest,\n  res: NextApiResponse\n) {\n  if (req.method !== \'POST\') {\n    return res.status(405).json({ error: \'Method not allowed\' })\n  }\n\n  const form = formidable({ multiples: false })\n\n  const [fields, files] = await new Promise<[formidable.Fields, formidable.Files]>(\n    (resolve, reject) => {\n      form.parse(req, (err, fields, files) => {\n        if (err) reject(err)\n        else resolve([fields, files])\n      })\n    }\n  )\n\n  const hash = fields.hash as string\n  const chunkIndex = fields.chunkIndex as string\n  const file = files.file as formidable.File\n\n  // 创建文件夹\n  const chunkDir = path.join(UPLOAD_DIR, hash)\n  await fs.mkdir(chunkDir, { recursive: true })\n\n  // 移动分片文件\n  const chunkPath = path.join(chunkDir, chunkIndex)\n  await fs.rename(file.filepath, chunkPath)\n\n  res.json({ success: true, chunkIndex: Number(chunkIndex) })\n}\n\n// api/upload/merge.ts\nexport default async function handler(\n  req: NextApiRequest,\n  res: NextApiResponse\n) {\n  const { fileHash, fileName, totalChunks } = req.body\n\n  const chunkDir = path.join(UPLOAD_DIR, fileHash)\n  const filePath = path.join(UPLOAD_DIR, `${fileHash}_${fileName}`)\n\n  // 合并分片\n  const writeStream = fs.createWriteStream(filePath)\n  \n  for (let i = 0; i < totalChunks; i++) {\n    const chunkPath = path.join(chunkDir, String(i))\n    const chunkBuffer = await fs.readFile(chunkPath)\n    writeStream.write(chunkBuffer)\n  }\n\n  writeStream.end()\n\n  // 清理分片目录\n  await fs.rm(chunkDir, { recursive: true })\n\n  res.json({ success: true, url: `/files/${fileHash}_${fileName}` })\n}\n\n// api/upload/verify.ts\nexport default async function handler(\n  req: NextApiRequest,\n  res: NextApiResponse\n) {\n  const { fileHash, totalChunks } = req.body\n\n  const filePath = path.join(UPLOAD_DIR, fileHash)\n  const chunkDir = path.join(UPLOAD_DIR, fileHash)\n\n  // 检查文件是否已存在（秒传）\n  try {\n    const files = await fs.readdir(UPLOAD_DIR)\n    const exists = files.some(f => f.startsWith(fileHash + \'_\'))\n    \n    if (exists) {\n      return res.json({ uploaded: true, uploadedChunks: [] })\n    }\n  } catch {}\n\n  // 检查已上传的分片\n  const uploadedChunks: number[] = []\n  \n  try {\n    const chunks = await fs.readdir(chunkDir)\n    chunks.forEach(chunk => {\n      uploadedChunks.push(Number(chunk))\n    })\n  } catch {}\n\n  res.json({ uploaded: false, uploadedChunks })\n}\n```\n### 性能优化要点\n优化策略	说明	效果\nWeb Worker 计算 Hash	避免主线程阻塞	页面保持流畅\n多 Worker 并行计算	利用多核 CPU	Hash 速度提升 4-8 倍\n动态分片大小	根据文件大小调整	平衡请求数和效率\n并发控制池	限制同时上传数	避免带宽拥塞\n断点续传	IndexedDB 存储进度	上传中断可恢复\n秒传检测	服务端 Hash 比对	重复文件无需上传\n分片重试	失败自动重试	提高上传成功率\n### 总结\n\"\n大文件上传的核心在于将复杂问题拆解：用分片解决大小问题，用多线程解决性能问题，用断点续传解决稳定性问题，用秒传解决效率问题。\n\n通过 Web Worker 多线程技术，我们可以在不阻塞主线程的情况下完成文件 Hash 计算；结合分片上传、并发控制和断点续传机制，可以实现稳定、高效的大文件上传功能。这套方案适用于云盘、视频平台、文件共享等需要处理大文件的场景。','深入探索 Web Worker 多线程技术，实现高性能大文件分片上传、断点续传与秒传功能','http://localhost:8088/uploads/2026/03/02667213-d824-4d9c-b887-a5810b5326a5.png',6,2,0,'published',31,19498,NULL,NULL,0,NULL,'2026-03-12 16:06:03','2026-03-12 16:06:03'),(6,'IntersectionObserver 图片懒加载：从原理到 React Hook 封装','深入剖析基于 IntersectionObserver 的图片懒加载实现，掌握自定义 useLazyLoad Hook 的设计与优化技巧','## IntersectionObserver 图片懒加载实现\n图片懒加载是前端性能优化的核心手段之一。通过延迟加载视口外的图片，可以显著减少首屏请求数量、降低带宽消耗。本文将深入分析一个基于 IntersectionObserver API 的 React 懒加载 Hook 实现，从观察器配置到生命周期管理，逐步拆解每一个设计决策。\n\n### 核心 API：IntersectionObserver\nIntersectionObserver 是浏览器原生提供的交叉观察器 API，用于异步检测目标元素与祖先元素（或视口）的交叉状态。相比传统的 scroll 事件监听方案，它具有更好的性能表现。\n\n#### 传统方案 vs IntersectionObserver\n```typescript\n// 传统方案：监听 scroll 事件（性能差）\nwindow.addEventListener(\"scroll\", () => {\n	images.forEach(img => {\n		const rect = img.getBoundingClientRect();\n		if (rect.top < window.innerHeight) {\n			img.src = img.dataset.src; // 频繁触发，需要节流\n		}\n	});\n});\n\n// 现代方案：IntersectionObserver（性能好）\nconst observer = new IntersectionObserver(entries => {\n	entries.forEach(entry => {\n		if (entry.isIntersecting) {\n			const img = entry.target as HTMLImageElement;\n			img.src = img.dataset.src!;\n			observer.unobserve(img); // 异步回调，浏览器优化调度\n		}\n	});\n});\n```\n#### 性能对比\n特性	scroll 事件方案	IntersectionObserver\n触发频率	极高，需手动节流	浏览器智能调度\n主线程阻塞	同步执行，阻塞渲染	异步回调，不阻塞\n计算方式	手动 getBoundingClientRect	浏览器内部优化\n代码复杂度	需要节流 + 手动计算	声明式 API，简洁\n兼容性	全兼容	现代浏览器均支持\n### 观察器配置解析\n```typescript\nconst observerOptions: IntersectionObserverInit = {\n	rootMargin: \"100px 0px\",\n};\n```\n#### IntersectionObserverInit 完整配置\n属性	类型	默认值	说明\nroot	Element | null	null（视口）	观察的根元素，null 表示浏览器视口\nrootMargin	string	\"0px\"	根元素的外边距，扩展/缩小观察区域\nthreshold	number | number[]	0	触发回调的交叉比例阈值\n#### rootMargin 的作用\nrootMargin 示意图 - 视口上下各扩展 100px 的预加载区域\nrootMargin 示意图 - 视口上下各扩展 100px 的预加载区域\n\n### useLazyLoad Hook 完整实现\n#### 核心代码\n```typescript\n\"use client\";\nimport { useRef, RefObject, useEffect } from \"react\";\n\n// 观察器配置：上下各扩展 100px 预加载区域\nconst observerOptions: IntersectionObserverInit = {\n	rootMargin: \"100px 0px\",\n};\n\nfunction useLazyLoad(ref: RefObject<HTMLElement | null>, data: any[]) {\n	// 持久化存储观察器实例，跨渲染周期保持引用\n	const observerRef = useRef<IntersectionObserver | null>(null);\n\n	// 懒加载回调函数\n	const lazyLoadCallback: IntersectionObserverCallback = entries => {\n		entries.forEach(entry => {\n			if (entry.isIntersecting) {\n				const img = entry.target as HTMLImageElement;\n				const realSrc = img.getAttribute(\"data-src\");\n				if (realSrc) {\n					img.src = realSrc; // 设置真实图片地址\n					img.removeAttribute(\"data-src\"); // 移除 data-src 标记\n					observerRef.current?.unobserve(img); // 停止监听已加载的图片\n				}\n			}\n		});\n	};\n\n	const initLazyLoad = () => {\n		// 1. 环境判断：仅客户端执行\n		if (typeof window === \"undefined\" || !window.IntersectionObserver) return;\n		// 2. 前置校验：ref 不存在/数据未就绪，直接返回\n		if (!ref.current || !data || data.length === 0) return;\n		// 3. 销毁旧的观察器（避免重复监听）\n		if (observerRef.current) {\n			observerRef.current.disconnect();\n		}\n		// 4. 初始化新的观察器\n		observerRef.current = new IntersectionObserver(\n			lazyLoadCallback,\n			observerOptions\n		);\n		// 5. 监听容器内所有带 data-src 的图片\n		const images = ref.current.querySelectorAll(\"img[data-src]\");\n		images.forEach(img => observerRef.current!.observe(img));\n	};\n\n	// 核心：监听 ref + 异步数据 的变化，数据就绪后初始化懒加载\n	useEffect(() => {\n		initLazyLoad();\n		return () => {\n			if (observerRef.current) {\n				observerRef.current.disconnect();\n				observerRef.current = null;\n			}\n		};\n	}, [ref, data]);\n}\n```\n#### 执行流程图\n执行流程图 - 从组件挂载到数据加载再到懒加载初始化的完整生命周期\n执行流程图 - 从组件挂载到数据加载再到懒加载初始化的完整生命周期\n\n### 关键设计点详解\n#### 1. data-src 占位模式\n```typescript\n// JSX 中：使用 data-src 存储真实地址，不设置 src\n<img\n	data-src={`https://picsum.photos/200/${180 + index}`}\n	alt=\"懒加载图片\"\n	style={{ width: \"200px\", height: \"200px\" }}\n/>;\n\n// 回调中：进入视口时替换为真实地址\nconst realSrc = img.getAttribute(\"data-src\");\nif (realSrc) {\n	img.src = realSrc; // 触发浏览器加载图片\n	img.removeAttribute(\"data-src\"); // 清除标记，避免重复加载\n	observerRef.current?.unobserve(img); // 取消监听，释放资源\n}\n```\n步骤	操作	目的\n初始	data-src=\"url\", 无 src	阻止浏览器自动请求图片\n进入视口	img.src = realSrc	触发浏览器开始加载图片\n清除标记	removeAttribute(\"data-src\")	防止重复触发加载\n取消监听	unobserve(img)	释放观察器资源\n#### 2. 异步数据驱动的初始化时机\n```typescript\nuseEffect(() => {\n	initLazyLoad();\n	return () => {\n		/* cleanup */\n	};\n}, [ref, data]); // 依赖 ref 和 data\n```\n为什么要将 data 作为依赖项？\n\n时间线：\n  0ms    → 组件挂载，data = []，DOM 中无 <img> 元素\n  1000ms → 异步数据返回，data = [1...100]，DOM 渲染 100 个 <img>\n如果不监听 data，观察器只会在组件挂载时初始化（此时 DOM 中没有图片），导致懒加载永远不会生效。\n\n#### 3. 观察器生命周期管理\n```typescript\nconst observerRef = useRef<IntersectionObserver | null>(null);\n\nconst initLazyLoad = () => {\n	// 销毁旧的观察器（避免重复监听）\n	if (observerRef.current) {\n		observerRef.current.disconnect();\n	}\n	// 创建新的观察器\n	observerRef.current = new IntersectionObserver(\n		lazyLoadCallback,\n		observerOptions\n	);\n	// ...\n};\n\n// useEffect cleanup\nreturn () => {\n	if (observerRef.current) {\n		observerRef.current.disconnect(); // 断开所有监听\n		observerRef.current = null; // 释放引用\n	}\n};\n```\n生命周期阶段	操作	目的\ndata 变化时	disconnect() 旧观察器	避免重复监听同一元素\n重新初始化	创建新 IntersectionObserver	监听新渲染的图片\n组件卸载时	disconnect() + 置 null	防止内存泄漏\n#### 4. 环境兼容性处理\n```typescript\n// SSR 安全检查\nif (typeof window === \"undefined\" || !window.IntersectionObserver) return;\n```\ntypeof window === \'undefined\'：防止 Node.js 服务端渲染时报错\n!window.IntersectionObserver：兼容不支持该 API 的旧浏览器\n### 使用示例\n#### 基础用法\n```typescript\nexport default function ImageGallery() {\n	const containerRef = useRef<HTMLDivElement>(null);\n	const [images, setImages] = useState<string[]>([]);\n\n	// 自定义 Hook：传入容器 ref 和数据\n	useLazyLoad(containerRef, images);\n\n	useEffect(() => {\n		// 模拟异步加载图片列表\n		fetchImages().then(setImages);\n	}, []);\n\n	return (\n		<div ref={containerRef}>\n			{images.map((url, index) => (\n				<img\n					key={index}\n					data-src={url} // 真实地址存储在 data-src\n					alt={`图片 ${index + 1}`}\n					style={{ width: \"200px\", height: \"200px\" }}\n				/>\n			))}\n		</div>\n	);\n}\n```\n#### 完整 Demo 组件\n```typescript\n\"use client\";\nimport { useRef, useEffect, useState } from \"react\";\n\nexport default function Test() {\n	const container = useRef<HTMLDivElement>(null);\n	const containerRef = useRef<HTMLDivElement>(null);\n\n	const [options, setOptions] = useState<Array<number | string>>([]);\n	useLazyLoad(containerRef, options);\n\n	useEffect(() => {\n		// 模拟 1 秒后异步加载 100 条数据\n		new Promise(resolve => {\n			setTimeout(() => {\n				setOptions(\n					Array(100)\n						.fill(\"\")\n						.map((_, i) => i + 1)\n				);\n				resolve(true);\n			}, 1000);\n		});\n	}, []);\n\n	return (\n		<div ref={containerRef} className=\"flex justify-center pt-8\">\n			<div className=\"grid gap-1 items-center\">\n				{options.map((item, index) => (\n					<img\n						data-src={`https://picsum.photos/200/${180 + index}`}\n						alt=\"懒加载图片\"\n						style={{ width: \"200px\", height: \"200px\" }}\n						key={index}\n					/>\n				))}\n			</div>\n		</div>\n	);\n}\n```\n### 优化建议\n#### 1. 添加加载占位符\n```typescript\n// 使用 CSS 占位，避免图片加载时的布局抖动\n<img\n	data-src={url}\n	alt=\"图片\"\n	className=\"bg-gray-200 animate-pulse\" // 骨架屏效果\n	style={{ width: \"200px\", height: \"200px\" }}\n/>\n```\n#### 2. 添加加载失败处理\n```typescript\nconst lazyLoadCallback: IntersectionObserverCallback = entries => {\n	entries.forEach(entry => {\n		if (entry.isIntersecting) {\n			const img = entry.target as HTMLImageElement;\n			const realSrc = img.getAttribute(\"data-src\");\n			if (realSrc) {\n				// 加载失败处理\n				img.onerror = () => {\n					img.src = \"/fallback-image.png\"; // 兜底图片\n				};\n				img.src = realSrc;\n				img.removeAttribute(\"data-src\");\n				observerRef.current?.unobserve(img);\n			}\n		}\n	});\n};\n```\n#### 3. 支持自定义 rootMargin\n```typescript\nfunction useLazyLoad(\n	ref: RefObject<HTMLElement | null>,\n	data: any[],\n	rootMargin: string = \"100px 0px\" // 允许自定义预加载距离\n) {\n	// ...\n	observerRef.current = new IntersectionObserver(lazyLoadCallback, {\n		rootMargin,\n	});\n}\n\n// 使用：更大的预加载区域\nuseLazyLoad(containerRef, images, \"300px 0px\");\n```\n#### 4. 考虑原生 loading=\"lazy\"\n```typescript\n// 现代浏览器已原生支持懒加载\n<img src={url} loading=\"lazy\" alt=\"图片\" />\n\n// 但自定义实现的优势：\n// 1. 更精细的控制（自定义 rootMargin、回调逻辑）\n// 2. 配合异步数据加载\n// 3. 统一的加载状态管理\n```\n### 常见面试题\n#### 1. 为什么用 IntersectionObserver 而不是 scroll 事件？\nscroll 事件触发频率极高（每帧都可能触发），即使加了节流也需要频繁调用 getBoundingClientRect()，会触发浏览器重排。IntersectionObserver 是浏览器原生优化的异步 API，在独立线程中计算交叉状态，不阻塞主线程。\n\n#### 2. 为什么要在 useEffect 的 cleanup 中 disconnect？\n如果组件卸载时不断开观察器，观察器仍会持有对已卸载 DOM 元素的引用，导致内存泄漏。disconnect() 会停止所有监听并释放内部资源。\n\n#### 3. data-src 方案有什么局限性？\n需要手动管理 data-src 和 src 的切换\n不设置 src 的图片没有原生的加载/错误事件\nSEO 不友好（搜索引擎爬虫可能看不到真实图片地址）\n不适用于 CSS 背景图\n#### 4. 如何处理动态新增的图片？\n当前实现依赖 data 变化重新初始化观察器。如果图片是通过其他方式动态添加的（如无限滚动），可以使用 MutationObserver 监听 DOM 变化，自动为新增图片注册观察。\n\n### 总结\n\"\n基于 IntersectionObserver 的图片懒加载是现代前端性能优化的标准方案。将其封装为 React Hook，可以实现声明式的懒加载能力，同时保持良好的可复用性。\n\n实现懒加载 Hook 的核心要点：\n\ndata-src 占位模式 - 阻止浏览器自动加载，按需触发\n异步数据驱动 - 将数据作为 useEffect 依赖，确保 DOM 就绪后初始化\n观察器生命周期 - 数据变化时重建，组件卸载时销毁，防止内存泄漏\n预加载策略 - 通过 rootMargin 提前加载即将进入视口的图片\n在实际项目中，建议优先考虑浏览器原生 loading=\"lazy\" 属性，只有在需要更精细控制（自定义预加载距离、配合异步数据、统一加载状态）时才使用自定义 IntersectionObserver 方案。','深入剖析基于 IntersectionObserver 的图片懒加载实现，掌握自定义 useLazyLoad Hook 的设计与优化技巧','http://localhost:8088/uploads/2026/03/e7bd2f88-7b4a-4fed-869e-57d33de9bece.jpg',6,2,0,'published',32,5761,NULL,NULL,0,NULL,'2026-03-13 00:01:31','2026-03-13 00:01:31');
/*!40000 ALTER TABLE `posts` ENABLE KEYS */;
UNLOCK TABLES;

--
-- Table structure for table `series`
--

DROP TABLE IF EXISTS `series`;
/*!40101 SET @saved_cs_client     = @@character_set_client */;
/*!50503 SET character_set_client = utf8mb4 */;
CREATE TABLE `series` (
  `id` bigint NOT NULL AUTO_INCREMENT,
  `name` varchar(100) COLLATE utf8mb4_unicode_ci NOT NULL,
  `slug` varchar(100) COLLATE utf8mb4_unicode_ci NOT NULL,
  `description` text COLLATE utf8mb4_unicode_ci,
  `cover_image` varchar(500) COLLATE utf8mb4_unicode_ci DEFAULT NULL,
  `created_at` datetime DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  UNIQUE KEY `slug` (`slug`)
) ENGINE=InnoDB AUTO_INCREMENT=5 DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
/*!40101 SET character_set_client = @saved_cs_client */;

--
-- Dumping data for table `series`
--

LOCK TABLES `series` WRITE;
/*!40000 ALTER TABLE `series` DISABLE KEYS */;
INSERT INTO `series` (`id`, `name`, `slug`, `description`, `cover_image`, `created_at`) VALUES (2,'性能优化','performanceOptimization','性能优化','','2026-03-12 16:10:44');
/*!40000 ALTER TABLE `series` ENABLE KEYS */;
UNLOCK TABLES;

--
-- Table structure for table `settings`
--

DROP TABLE IF EXISTS `settings`;
/*!40101 SET @saved_cs_client     = @@character_set_client */;
/*!50503 SET character_set_client = utf8mb4 */;
CREATE TABLE `settings` (
  `id` bigint NOT NULL AUTO_INCREMENT,
  `setting_key` varchar(100) COLLATE utf8mb4_unicode_ci NOT NULL,
  `setting_value` text COLLATE utf8mb4_unicode_ci,
  `created_at` datetime DEFAULT CURRENT_TIMESTAMP,
  `updated_at` datetime DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  UNIQUE KEY `setting_key` (`setting_key`)
) ENGINE=InnoDB AUTO_INCREMENT=7 DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
/*!40101 SET character_set_client = @saved_cs_client */;

--
-- Dumping data for table `settings`
--

LOCK TABLES `settings` WRITE;
/*!40000 ALTER TABLE `settings` DISABLE KEYS */;
INSERT INTO `settings` (`id`, `setting_key`, `setting_value`, `created_at`, `updated_at`) VALUES (1,'siteName','huwx BLOG','2026-03-13 14:18:13','2026-03-13 14:18:51'),(2,'siteDescription','分享技术知识与学习心得','2026-03-13 14:18:13','2026-03-13 14:18:51'),(3,'siteUrl','https://example.com','2026-03-13 14:18:13','2026-03-13 14:18:13'),(4,'postsPerPage','10','2026-03-13 14:18:13','2026-03-13 14:18:13'),(5,'enableComments','true','2026-03-13 14:18:13','2026-03-13 14:18:13'),(6,'analyticsId','','2026-03-13 14:18:13','2026-03-13 14:18:13');
/*!40000 ALTER TABLE `settings` ENABLE KEYS */;
UNLOCK TABLES;

--
-- Table structure for table `tags`
--

DROP TABLE IF EXISTS `tags`;
/*!40101 SET @saved_cs_client     = @@character_set_client */;
/*!50503 SET character_set_client = utf8mb4 */;
CREATE TABLE `tags` (
  `id` bigint NOT NULL AUTO_INCREMENT,
  `name` varchar(100) COLLATE utf8mb4_unicode_ci NOT NULL,
  `slug` varchar(100) COLLATE utf8mb4_unicode_ci NOT NULL,
  PRIMARY KEY (`id`),
  UNIQUE KEY `slug` (`slug`)
) ENGINE=InnoDB AUTO_INCREMENT=9 DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
/*!40101 SET character_set_client = @saved_cs_client */;

--
-- Dumping data for table `tags`
--

LOCK TABLES `tags` WRITE;
/*!40000 ALTER TABLE `tags` DISABLE KEYS */;
INSERT INTO `tags` (`id`, `name`, `slug`) VALUES (5,'文件上传','file-upload'),(6,'性能优化','performance-optimization'),(8,'Demo Tag','demo-tag');
/*!40000 ALTER TABLE `tags` ENABLE KEYS */;
UNLOCK TABLES;

--
-- Table structure for table `users`
--

DROP TABLE IF EXISTS `users`;
/*!40101 SET @saved_cs_client     = @@character_set_client */;
/*!50503 SET character_set_client = utf8mb4 */;
CREATE TABLE `users` (
  `id` bigint NOT NULL AUTO_INCREMENT,
  `email` varchar(255) COLLATE utf8mb4_unicode_ci NOT NULL,
  `password_hash` varchar(255) COLLATE utf8mb4_unicode_ci NOT NULL,
  `name` varchar(100) COLLATE utf8mb4_unicode_ci DEFAULT NULL,
  `role` varchar(50) COLLATE utf8mb4_unicode_ci DEFAULT 'USER',
  `created_at` datetime DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  UNIQUE KEY `email` (`email`)
) ENGINE=InnoDB AUTO_INCREMENT=2 DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
/*!40101 SET character_set_client = @saved_cs_client */;

--
-- Dumping data for table `users`
--

LOCK TABLES `users` WRITE;
/*!40000 ALTER TABLE `users` DISABLE KEYS */;
INSERT INTO `users` (`id`, `email`, `password_hash`, `name`, `role`, `created_at`) VALUES (1,'admin@blog.com','$2a$10$9DYYghdHE1uNPbZ83AbosOOScQGMbkvs1t492P9bz4OFKnXadIJHe','admin','ADMIN','2026-03-11 16:45:55');
/*!40000 ALTER TABLE `users` ENABLE KEYS */;
UNLOCK TABLES;
/*!40103 SET TIME_ZONE=@OLD_TIME_ZONE */;

/*!40101 SET SQL_MODE=@OLD_SQL_MODE */;
/*!40014 SET FOREIGN_KEY_CHECKS=@OLD_FOREIGN_KEY_CHECKS */;
/*!40014 SET UNIQUE_CHECKS=@OLD_UNIQUE_CHECKS */;
/*!40101 SET CHARACTER_SET_CLIENT=@OLD_CHARACTER_SET_CLIENT */;
/*!40101 SET CHARACTER_SET_RESULTS=@OLD_CHARACTER_SET_RESULTS */;
/*!40101 SET COLLATION_CONNECTION=@OLD_COLLATION_CONNECTION */;
/*!40111 SET SQL_NOTES=@OLD_SQL_NOTES */;

-- Dump completed on 2026-03-13 17:38:21
